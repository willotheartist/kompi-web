"use client";

/**
 * CreateWorkspaceModal
 * --------------------
 * Disabled in single-workspace mode.
 */

type Props = {
  open: boolean;
  onOpenChange: (open: boolean) => void;
};

export function CreateWorkspaceModal(_props: Props) {
  return null;
}
