"use client";

/**
 * WorkspaceSwitcherDialog
 * ----------------------
 * Disabled in single-workspace mode.
 */

type Props = {
  open: boolean;
  onOpenChange: (open: boolean) => void;
};

export function WorkspaceSwitcherDialog(_props: Props) {
  return null;
}
